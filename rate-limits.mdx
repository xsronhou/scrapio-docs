---
title: Rate Limits & Credits
description: How the credit system works, per-surface costs, and what happens when you hit a limit.
---

## Credit system

Every API call consumes credits. Credits are deducted when a request completes successfully. Failed requests (4xx errors caused by bad input) are not charged.

Your balance resets monthly, at the start of your billing cycle. Credits do not roll over — unused credits expire at the end of the cycle, on every plan including paid ones.

## Per-surface credit costs

| Surface | Credits per call | Notes |
|---------|-----------------|-------|
| Fetch (no JS) | 1 | Static HTML fetch |
| Fetch (JS rendering) | 2 | `render_js: true` |
| Fetch + extract | 2–5 | +1–3 depending on extraction mode |
| Crawl | 1 per page crawled | Charged per page, not per job |
| Interact | 3 | Per interaction session |
| Search (no fetch) | 12 | Google SERP only |
| Search + fetch results | 12 + 1 per fetched result | |
| Fast Search | 15 | Organic results only, no HTML capture. Requires a Starter plan or higher |
| YouTube search | 1 | |
| YouTube video details | 1 | |
| YouTube subtitles | 1 | |
| Google SERP search | 15 | Requires a Pro plan or higher |
| Amazon product / search | 10 | Requires a Starter plan or higher |
| Walmart search | 10 | Requires a Starter plan or higher |
| Booking.com search, property, reviews | 15 | Requires a Starter plan or higher |
| Agoda search, property, reviews | 15 | Requires a Starter plan or higher |
| Agoda search page 2+, reviews page 2+ | 25 | Requires a Starter plan or higher |

`GET /v1/walmart/product` is temporarily disabled while we work on reliability for individual Walmart product pages. Search and search/crawl are unaffected.

## Plan limits

| Plan | Monthly credits | Notes |
|------|--------------|-------|
| Free | 1,000 | Resets monthly. No proxy access — see below. |
| Starter | 25,000 | Unlocks dedicated APIs (Amazon, Walmart, Booking.com, Agoda) and Fast Search. |
| Pro | 100,000 | Everything in Starter, plus Google SERP search. |
| Scale | 500,000 | Everything in Pro, plus custom rate limits. |
| Enterprise | Custom | SLA + dedicated capacity |

Credits do not roll over on any plan — unused credits expire at the end of the billing cycle.

### Free plan does not include proxy access

The Free plan never routes requests through a datacenter or residential proxy — only direct HTTP and stealth-browser access. Most targets work fine without a proxy, but sites with stronger anti-bot defenses may block Free-plan requests that would succeed on a paid plan. When this happens, the API returns `free_plan_proxy_required` (see [Error Reference](/errors)) instead of a generic block, so you know upgrading is the fix rather than retrying.

### Amazon, Walmart, Booking.com, Agoda, and Fast Search require a paid plan

Amazon, Walmart, Booking.com, Agoda, and Fast Search all require a Starter plan or higher. Google SERP search requires a Pro plan or higher. Requesting any of these on an ineligible plan returns `402 plan_upgrade_required`.

## Quota errors

When your credit balance runs out, the API returns:

```json
{
  "request_id": "...",
  "error": {
    "code": "credits_exhausted",
    "message": "Your plan's credit allowance has been exhausted. Buy a top-up or upgrade to continue."
  }
}
```

| Code | Status | When |
|------|--------|------|
| `credits_exhausted` | 402 | Balance is zero |

Credits are a hard cap, not billed overage — requests are blocked once your plan's included credits run out. Buy a top-up or upgrade to a higher plan to keep going.

## Checking your usage

Use the `usage` field in every successful inline response to see how many credits that call consumed:

```json
{
  "usage": { "credits": 2 }
}
```

Aggregate usage dashboards are available at [scrapio.dev](https://scrapio.dev).
