---
title: Dice
description: Search Dice.com tech job listings by keywords, with an optional free-text location.
---

<Note>
  Dice endpoints require a paid plan (Starter or higher). Without one, these endpoints return `402 plan_upgrade_required`. See [Pricing](/pricing) for details.
</Note>

## Endpoints

- `GET /v1/dice/jobs` — Search job listings by keywords, with an optional free-text location. Cursor-paginated.
